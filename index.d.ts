export function pluralize(someString: string): string;
export function pluralize(count: number, someString: string, options?: { withoutCount?: boolean }): string;

export function singularize(someString: string): string;
